#  В фермерском хозяйстве в Карелии выращивают чернику. Она растет на
# круглой грядке, причем кусты высажены только по окружности. Таким образом, у
# каждого куста есть ровно два соседних. Всего на грядке растет N кустов.
# Эти кусты обладают разной урожайностью, поэтому ко времени сбора на них
# выросло различное число ягод – на i-ом кусте выросло ai
#  ягод.
# В этом фермерском хозяйстве внедрена система автоматического сбора черники.
# Эта система состоит из управляющего модуля и нескольких собирающих модулей.
# Собирающий модуль за один заход, находясь непосредственно перед некоторым
# кустом, собирает ягоды с этого куста и с двух соседних с ним.
# Напишите программу для нахождения максимального числа ягод, которое может
# собрать за один заход собирающий модуль, находясь перед некоторым кустом
# заданной во входном файле грядки.
# 4 -> 1 2 3 4
# 9

arr = [1, 2, 3, 4]

# Введите ваше решение ниже

def max_berries(arr):
    n = len(arr)
    
    # Если в списке меньше трех элементов, мы не можем собрать с трех кустов, возвращаем сумму всех элементов
    if n < 3:
        return sum(arr)
    
    max_berries_collected = 0

    # Проходим по каждому кусту и вычисляем сумму ягод с этого куста и двух соседних кустов
    for i in range(n):
        # Используем модульную арифметику для циклического массива
        curr_sum = arr[i] + arr[(i + 1) % n] + arr[(i - 1) % n]
        # Обновляем максимальную сумму, если текущая сумма больше
        max_berries_collected = max(max_berries_collected, curr_sum)
    
    return max_berries_collected


print(max_berries(arr))
